os: linux
dist: xenial
language: generic

services:
  - docker

after_success:
  - docker build -t "filipdadgar/staticweb:latest" .
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push "filipdadgar/staticweb:latest"
  - echo "done pushing!"